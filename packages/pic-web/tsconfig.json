{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist",
    "jsx": "react-jsx",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    // promote-path remote tests use node:fs/crypto for `.env.local` loading (mirrors pic-adapter-supabase).
    "types": ["node", "vite/client"]
  },
  "include": ["src"]
}
